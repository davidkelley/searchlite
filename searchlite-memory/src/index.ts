// Public entry point for programmatic use. Most consumers invoke the CLI
// (`searchlite-memory`) or run it as an MCP server. Concrete exports (the
// memory store, embedder interface, etc.) are added as later stages land.

export const VERSION = "0.1.0";
